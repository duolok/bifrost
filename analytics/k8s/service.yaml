apiVersion: v1
kind: Service
metadata:
  name: bifrost-analytics
  namespace: bifrost-apps
  labels:
    app: bifrost-analytics
spec:
  type: ClusterIP
  selector:
    app: bifrost-analytics
  ports:
    - port: 80
      targetPort: 8090
