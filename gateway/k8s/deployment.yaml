apiVersion: apps/v1
kind: Deployment
metadata:
  name: bifrost-gateway
  namespace: bifrost-apps
  labels:
    app: bifrost-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bifrost-gateway
  template:
    metadata:
      labels:
        app: bifrost-gateway
    spec:
      serviceAccountName: bifrost-gateway
      containers:
        - name: gateway
          image: europe-central2-docker.pkg.dev/bifrost-platform/bifrost-platform/gateway:latest
          ports:
            - containerPort: 8080
          env:
            - name: BF_PORT
              value: "8080"
            - name: BF_ENV
              value: "prod"
            - name: BF_DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: bifrost-config
                  key: database_url
            - name: BF_GCP_PROJECT
              value: "bifrost-platform"
            - name: BF_AR_REPO
              value: "europe-central2-docker.pkg.dev/bifrost-platform/bifrost-apps"
            - name: BF_K8S_IN_CLUSTER
              value: "true"
            - name: BF_K8S_NAMESPACE
              value: "bifrost-apps"
            - name: BF_VALIDATOR_URL
              value: "https://bifrost-validator-274528789467.europe-central2.run.app"
            - name: BF_REALTIME_URL
              value: "bifrost-realtime:50051"
            - name: BF_RABBITMQ_URL
              value: "amqp://bifrost:localdev@bifrost-rabbitmq:5672/"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
